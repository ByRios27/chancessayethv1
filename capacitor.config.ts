import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ventasdechances.app',
  appName: 'Ventas de Chances',
  webDir: 'dist'
};

export default config;
